---
- name: Delete flux_ssh_key_tempfile
  ansible.builtin.file:
    state: absent
    path: "{{ flux_ssh_key_tempfile.path }}"
  changed_when: false

- name: Delete kubecfg_tempfile
  ansible.builtin.file:
    state: absent
    path: "{{ kubecfg_tempfile.path }}"
  changed_when: false
