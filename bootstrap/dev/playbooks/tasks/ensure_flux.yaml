---
- name: Ensure that flux is installed
  ansible.builtin.command: flux version --client
  register: flux_version
  changed_when: false

- name: Get flux version
  ansible.builtin.set_fact:
    flux_version: "{{ flux_version.stdout | regex_replace('flux: ', '') }}"
